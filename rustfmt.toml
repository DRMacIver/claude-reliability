# Rustfmt configuration
edition = "2021"
max_width = 100
use_small_heuristics = "Max"
# imports_granularity and group_imports are nightly-only features
# Commented out to avoid warnings on stable toolchain
# imports_granularity = "Crate"
# group_imports = "StdExternalCrate"
reorder_imports = true
reorder_modules = true
newline_style = "Unix"
