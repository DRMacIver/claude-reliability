---
description: Cancel the active just-keep-working mode session
---

# Cancel Just Keep Working Mode

Remove the just-keep-working session files to stop the current session:

```bash
rm -f .claude/jkw-state.local.yaml .claude/jkw-session.local.md
```

Confirm cancellation to the user.
