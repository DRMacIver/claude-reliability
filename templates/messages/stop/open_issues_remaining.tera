{# Message shown when trying to exit with open issues.

Variables:
  - open_count: Number of open issues
  - problem_phrase: The phrase to use when hitting a problem
#}
# Tasks Remaining

There are {{ open_count }} task(s) still open. Run `list_tasks` to see what's left.

If you can continue, please work on the remaining tasks.

If you've genuinely hit a blocker that needs user input, say: "{{ problem_phrase }}"
